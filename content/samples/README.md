# Sample mission packs

Versioned mission Brief/Debrief sources used for local development and documentation—not the production default.

| Pack | Path | Used by |
|------|------|---------|
| Example class | `example-class/` | `scripts/bootstrap_sample_class.sh` (copies into `content/missions/` + example SQL seed) |

Add new packs here when you need additional seeded curricula; keep `content/missions/` for live operator-authored missions plus `basic-training`.
